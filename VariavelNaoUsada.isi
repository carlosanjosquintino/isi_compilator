programa

    declare numero a,k,x.
    declare texto t.

    escreva("Digite um número").
    leia(a).
    escreva("Digite um texto").
    leia(t).

    k := a+3.


    escreva(k).
    escreva("Escrita do texto passado").
    escreva(t).

fimprog.