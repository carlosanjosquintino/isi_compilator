programa
    declare numero a, b,c.

    escreva("Digite o numero de repeticao do enquanto ").

    a := 0.
    leia(b).


    enquanto (a < b)
    {
        escreva (a).
        a := a+1.
    }

    a := 0.
    escreva("Digite o numero de repeticao do faca enquanto ").
    leia(c).
    faca {
        escreva (a).
        a := a+1.
    } enquanto (a < c).

fimprog.